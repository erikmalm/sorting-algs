import React from "react";

function MergeSortVisualizationView(props) {
  return (
    <div className="merge-sort-visualization">
      {props.children}
    </div>
  );
}

export default MergeSortVisualizationView;